const Vision = (): React.JSX.Element => {
  return (
    <p>
      Always open to side projects and creative collaboration — especially with
      kindred spirits.
    </p>
  );
};

export default Vision;
